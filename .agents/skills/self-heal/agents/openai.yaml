interface:
  display_name: "Self Heal"
  short_description: "Repo-specific patterns and workflows for self-heal"
  default_prompt: "Use the self-heal repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true